{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "MongoDB local",
        "type": "shell",
        "command": "docker compose up -d",
        "options": {
          "cwd": "${workspaceFolder}"
        }
      }
    ]
  }